import { initialStatuses } from "./initialStatus";
export const StatusSlice = (state = [], action) => {
    switch (action.type) {
    


        default: return state
    }

}
export function showStatus(state) {
    return state.statuses
}
